<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Patra is a client for a server: without this it cannot open a socket
         at all. Flutter's template declares it only in the debug and profile
         manifests, where it exists for the tool's own hot-reload channel, so
         a release build silently shipped with no network — every request
         failing at the connection, on any address, local or not. Nothing in
         the Dart tests or `flutter analyze` can see this; the CI build
         asserts it is in the bundle instead. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- The prompt in front of a locked profile. Declared rather than left to
         the plugin's own manifest merge so it is readable here beside the one
         other permission this app asks for; it is granted at install and is
         never prompted for. Without a sensor or an enrolment the API simply
         reports that it cannot be used, which is the ordinary case and not an
         error — the PIN is what a profile is really locked with. -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC"/>

    <application
        android:label="Patra"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
